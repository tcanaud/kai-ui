qa_version: "1.0"

feature_id: "001-terminal-panel-xterm-js-tmux-integration"
generated: "2026-02-20T00:00:00Z"
generator: "qa.plan"

checksums:
  spec_md:
    path: "specs/001-terminal-panel-xterm-js-tmux-integration/spec.md"
    sha256: "e6adb9f8c501e797ee7e7d7dccabca0c4ad8530fb9591dc21baee3d8445b563c"
  agreement_yaml:
    path: ".agreements/001-terminal-panel-xterm-js-tmux-integration/agreement.yaml"
    sha256: "a01dc6f0bd3eb9ee9210b016670825082821bccac7484a85b706597c5ae63674"

scripts:
  - filename: "test-terminal-renders-shell-prompt.sh"
    criterion_ref: "US1.AC1"
    criterion_text: "Given a session is loaded in the UI, When the terminal panel renders, Then a live terminal with a shell prompt is displayed in the panel slot (replacing the placeholder)."
    type: "acceptance"
  - filename: "test-command-execution.sh"
    criterion_ref: "US1.AC2"
    criterion_text: "Given the terminal panel is displayed, When the user types a command and presses Enter, Then the command executes and output is displayed in the terminal."
    type: "acceptance"
  - filename: "test-interactive-tui.sh"
    criterion_ref: "US1.AC3"
    criterion_text: "Given the terminal panel is displayed, When the user runs an interactive program (e.g., top, vim), Then the terminal correctly handles interactive/full-screen TUI rendering."
    type: "acceptance"
  - filename: "test-tmux-auto-attach.sh"
    criterion_ref: "US2.AC1"
    criterion_text: "Given a session with an existing tmux session, When the terminal panel loads, Then it automatically attaches to the existing tmux session showing previous state."
    type: "acceptance"
  - filename: "test-tmux-new-session.sh"
    criterion_ref: "US2.AC2"
    criterion_text: "Given a session without an existing tmux session, When the terminal panel loads for the first time, Then a new tmux session is created and attached."
    type: "acceptance"
  - filename: "test-state-persistence.sh"
    criterion_ref: "US2.AC3"
    criterion_text: "Given the user is connected to a tmux session, When the user refreshes the browser or navigates away and returns, Then the terminal reconnects to the same tmux session with all state preserved."
    type: "acceptance"
  - filename: "test-auto-reconnect.sh"
    criterion_ref: "US2.AC4"
    criterion_text: "Given the WebSocket connection drops unexpectedly, When the connection is restored, Then the terminal re-attaches to the tmux session automatically without user intervention."
    type: "acceptance"
  - filename: "test-scrollback.sh"
    criterion_ref: "US3.AC1"
    criterion_text: "Given the terminal has output exceeding the visible area, When the user scrolls up, Then previous output is visible and the user can scroll through the full scrollback buffer."
    type: "acceptance"
  - filename: "test-copy-to-clipboard.sh"
    criterion_ref: "US3.AC2"
    criterion_text: "Given text is visible in the terminal, When the user selects text and triggers copy, Then the selected text is placed on the system clipboard."
    type: "acceptance"
  - filename: "test-paste-from-clipboard.sh"
    criterion_ref: "US3.AC3"
    criterion_text: "Given text is on the system clipboard, When the user triggers paste, Then the clipboard content is inserted into the terminal at the cursor position."
    type: "acceptance"
  - filename: "test-cyberpunk-colors.sh"
    criterion_ref: "US4.AC1"
    criterion_text: "Given the terminal panel is displayed, When the user views the terminal, Then the terminal background, foreground, and cursor colors match the kai cyberpunk palette."
    type: "acceptance"
  - filename: "test-ansi-color-mapping.sh"
    criterion_ref: "US4.AC2"
    criterion_text: "Given the terminal displays colored output, When ANSI colors are rendered, Then they map to the cyberpunk palette variants (neon cyan, neon violet, etc.)."
    type: "acceptance"
  - filename: "test-visual-integration.sh"
    criterion_ref: "US4.AC3"
    criterion_text: "Given the terminal is embedded in the panel slot, When viewed alongside other panels, Then the terminal visually integrates with the panel header bar, borders, and surrounding UI elements."
    type: "acceptance"
  - filename: "test-resize-dimensions.sh"
    criterion_ref: "US5.AC1"
    criterion_text: "Given the terminal panel is displayed, When the browser window is resized, Then the terminal adjusts its column and row count to fill the panel and notifies the backend of the new dimensions."
    type: "acceptance"
  - filename: "test-tui-resize-render.sh"
    criterion_ref: "US5.AC2"
    criterion_text: "Given a full-screen TUI application is running, When the panel is resized, Then the TUI application re-renders correctly at the new dimensions without artifacts."
    type: "acceptance"
  - filename: "test-resize-debounce.sh"
    criterion_ref: "US5.AC3"
    criterion_text: "Given the panel is in the mobile layout, When the terminal panel expands or collapses, Then the terminal dimensions update accordingly."
    type: "acceptance"
  - filename: "test-if1-react-component.sh"
    criterion_ref: "IF1"
    criterion_text: "React component rendering xterm.js in the terminal panel slot"
    type: "interface"
  - filename: "test-if2-websocket-endpoint.sh"
    criterion_ref: "IF2"
    criterion_text: "WebSocket bidirectional channel — raw text for I/O, JSON for resize/error/exit"
    type: "interface"
  - filename: "test-if3-health-endpoint.sh"
    criterion_ref: "IF3"
    criterion_text: "HTTP health endpoint on the sidecar server (port 3001)"
    type: "interface"

total_scripts: 19
by_type:
  acceptance: 16
  interface: 3
  edge_case: 0
