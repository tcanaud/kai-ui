# Playbook Index
# Auto-generated by @tcanaud/playbook init/update
# Lists all available playbooks in this project.

generated: "2026-02-20T00:00:00.000Z"

playbooks:
  - name: "auto-feature"
    file: "playbooks/auto-feature.yaml"
    description: "Full feature workflow from plan to PR"
    steps: 8
  - name: "auto-validate"
    file: "playbooks/auto-validate.yaml"
    description: "QA validation: plan and run"
    steps: 2
  - name: "backlog-to-pr"
    file: "playbooks/backlog-to-pr.yaml"
    description: "Full pipeline from an open backlog to a merged-ready PR — promote, specify, plan, implement, validate, and ship"
    steps: 12
  - name: "hotfix"
    file: "playbooks/hotfix.yaml"
    description: "Fast-track critical bugs — pick critical backlogs, implement the fix, run QA, and ship a PR without full spec cycle"
    steps: 6
  - name: "intention-to-pr"
    file: "playbooks/intention-to-pr.yaml"
    description: "End-to-end flow from product intention to pull request"
    steps: 12
  - name: "intention-to-pr-haiku"
    file: "playbooks/intention-to-pr-haiku.yaml"
    description: "End-to-end flow from product intention to pull request — all steps executed with haiku model"
    steps: 13
  - name: "knowledge-maintenance"
    file: "playbooks/knowledge-maintenance.yaml"
    description: "Refresh knowledge index, check freshness of all guides, and report stale or outdated documentation"
    steps: 3
  - name: "pm-digest"
    file: "playbooks/pm-digest.yaml"
    description: "Full PM pipeline — triage new feedbacks into backlogs, check data integrity, review backlogs, promote high-priority items to features, and produce a health dashboard"
    steps: 6
  - name: "qa-mcp-chrome"
    file: "playbooks/qa-mcp-chrome.yaml"
    description: "Adversarial QA session via MCP Chrome DevTools — test the app like a real user, then intake feedbacks for every finding"
    steps: 10
